<Window x:Class="Casper.DataForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Casper.DataForge" Height="720" Width="1240" MinHeight="560" MinWidth="900"
        WindowStartupLocation="CenterScreen" Background="#111318" Foreground="#E8EAF0">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#252A34"/>
            <Setter Property="Foreground" Value="#F4F6FA"/>
            <Setter Property="BorderBrush" Value="#3B4352"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#171A21"/>
            <Setter Property="Foreground" Value="#E8EAF0"/>
            <Setter Property="BorderBrush" Value="#343B48"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="SelectionBrush" Value="#365F91"/>
            <Setter Property="FontFamily" Value="Consolas, Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Casper.DataForge" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Auto" Click="AutoDirection_Click"/>
                <Button Content="RTL" Click="RtlDirection_Click"/>
                <Button Content="LTR" Click="LtrDirection_Click"/>
                <Button Content="JSON" Click="Json_Click"/>
                <Button Content="JSONL" Click="Jsonl_Click"/>
                <Button Content="Copy" Click="Copy_Click"/>
                <Button Content="Save" Click="Save_Click"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="SOURCE / المصدر" Foreground="#9FA8B8" Margin="2,0,0,6"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="JSON / JSONL" Foreground="#9FA8B8" Margin="2,0,0,6"/>

            <TextBox x:Name="InputTextBox" Grid.Row="1" Grid.Column="0"
                     AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     TextChanged="InputTextBox_TextChanged"/>

            <GridSplitter Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Width="6"
                          HorizontalAlignment="Stretch" Background="#252A34"/>

            <TextBox x:Name="OutputTextBox" Grid.Row="1" Grid.Column="2"
                     IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     FlowDirection="LeftToRight" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="2,10,0,0" Foreground="#8F9BAD"
                   Text="Ready"/>
    </Grid>
</Window>
